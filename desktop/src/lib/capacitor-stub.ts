// Empty stub — Capacitor plugins are dynamically imported and guarded by isMobileApp(),
// but Tauri builds need a resolvable module. This file is aliased in vite.config.ts
// only when TAURI_PLATFORM is set.
export default {};
